import 'package:flutter/material.dart';

class InscriptionTeacher extends StatelessWidget {
  const InscriptionTeacher({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
