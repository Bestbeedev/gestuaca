import 'package:flutter/material.dart';

class DashboardStudents extends StatelessWidget {
  const DashboardStudents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
